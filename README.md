# web-log-analytics-pyspark
Web Log Analytics with PySpark (SQL + RDD)  Big data pipeline that parses raw, unstructured web server log files and turns them into structured, queryable datasets — then runs a series of analytical queries using both Spark SQL and Spark RDDs, with results visualized using Pandas/Matplotlib.
